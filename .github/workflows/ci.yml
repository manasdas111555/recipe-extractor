name: CI Pipeline & Quality Gate

on:
  push:
    branches: [ Dev, staging, main ]
  pull_request:
    branches: [ Dev, staging, main ]

jobs:
  test-and-verify:
    name: Verify & Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [ '3.10', '3.11' ]

    steps:
      - name: Checkout Code Repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install System Utilities (ffmpeg)
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Automated Pre-Promotion Verification & Isolated Imports
        run: |
          python scripts/verify_promotion.py

      - name: Execute Full Unit Test Suite
        run: |
          python -m unittest discover -s tests -v
